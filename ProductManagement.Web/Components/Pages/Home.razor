@* 🎉 Welcome to ProductHub - Where products meet their destiny! Built with love, caffeine, and probably too many Stack Overflow visits 🎉 *@
@page "/"

<PageTitle>ProductHub - Dashboard</PageTitle>

<div class="hero-section">
    <div class="container">
        <div class="row justify-content-center text-center">
            <div class="col-lg-8">
                <div class="hero-content">
                    <h1 class="display-4 fw-bold mb-4">
                        <span class="text-gradient">Welcome to ProductHub</span>
                        <span class="wave-emoji">👋</span>
                    </h1>
                    <p class="lead mb-5 text-muted">
                        Your modern, blazing-fast product management solution built with 
                        <span class="fw-semibold text-primary">.NET Aspire</span>, 
                        <span class="fw-semibold text-info">Blazor</span>, and 
                        <span class="fw-semibold text-success">PostgreSQL</span>
                    </p>
                    
                    <div class="d-flex gap-3 justify-content-center flex-wrap">
                        <a href="/products" class="btn btn-primary btn-lg px-5 py-3 rounded-pill shadow-lg">
                            <i class="bi bi-box-seam-fill me-2"></i>
                            Manage Products
                        </a>
                        <button class="btn btn-outline-secondary btn-lg px-5 py-3 rounded-pill" onclick="scrollToFeatures()">
                            <i class="bi bi-info-circle me-2"></i>
                            Learn More
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="features-section" id="features">
    <div class="container">
        <div class="row">
            <div class="col-12 text-center mb-5">
                <h2 class="fw-bold">Why Choose ProductHub?</h2>
                <p class="text-muted">Built with modern technologies and best practices</p>
            </div>
        </div>
        
        <div class="row g-4">
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon bg-primary">
                        <i class="bi bi-lightning-charge-fill"></i>
                    </div>
                    <h4>Blazing Fast</h4>
                    <p class="text-muted">Built with Blazor Server-Side Rendering for optimal performance and SEO.</p>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon bg-success">
                        <i class="bi bi-shield-check-fill"></i>
                    </div>
                    <h4>Production Ready</h4>
                    <p class="text-muted">Comprehensive validation, error handling, and security best practices.</p>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon bg-info">
                        <i class="bi bi-gear-fill"></i>
                    </div>
                    <h4>Modern Stack</h4>
                    <p class="text-muted">.NET Aspire, FastEndpoints, Dapper, and PostgreSQL working in harmony.</p>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .hero-section {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 8rem 0;
        position: relative;
        overflow: hidden;
    }
    
    .hero-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="white" opacity="0.1"/><circle cx="75" cy="25" r="1" fill="white" opacity="0.05"/><circle cx="50" cy="50" r="1" fill="white" opacity="0.1"/><circle cx="25" cy="75" r="1" fill="white" opacity="0.05"/><circle cx="75" cy="75" r="1" fill="white" opacity="0.1"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
        pointer-events: none;
    }
    
    .hero-content {
        position: relative;
        z-index: 2;
    }
    
    .text-gradient {
        background: linear-gradient(45deg, #ffffff, #f8f9fa);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    
    .wave-emoji {
        display: inline-block;
        animation: wave 2s infinite;
        transform-origin: 70% 70%;
    }
    
    @@keyframes wave {
        0%, 100% { transform: rotate(0deg); }
        10%, 30% { transform: rotate(14deg); }
        20% { transform: rotate(-8deg); }
        40% { transform: rotate(-4deg); }
        50% { transform: rotate(10deg); }
        60% { transform: rotate(0deg); }
    }
    
    .features-section {
        padding: 6rem 0;
        background-color: #f8f9fa;
    }
    
    .feature-card {
        text-align: center;
        padding: 2rem;
        background: white;
        border-radius: 1rem;
        box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        height: 100%;
    }
    
    .feature-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 30px rgba(0,0,0,0.15);
    }
    
    .feature-icon {
        width: 4rem;
        height: 4rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        color: white;
        font-size: 1.5rem;
    }
    
    .btn-primary {
        background: linear-gradient(45deg, #667eea, #764ba2);
        border: none;
        transition: all 0.3s ease;
    }
    
    .btn-primary:hover {
        background: linear-gradient(45deg, #5a67d8, #6b46a4);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
    }
</style>

<script>
    function scrollToFeatures() {
        document.getElementById('features').scrollIntoView({ behavior: 'smooth' });
    }
</script>
